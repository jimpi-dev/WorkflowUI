import type { NodeSpec } from '../types';

export const EasySeedSpec: NodeSpec = {
    classType: 'easy seed',
    fixedInputs: {
        seed: {
            type: 'seed',
            label: 'Seed'
        }
    }
};
