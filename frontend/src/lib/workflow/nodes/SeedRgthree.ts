import type { NodeSpec } from '../types';

export const SeedRgthreeSpec: NodeSpec = {
    classType: 'Seed (rgthree)',
    fixedInputs: {
        seed: {
            type: 'seed',
            label: 'Seed'
        }
    }
};
